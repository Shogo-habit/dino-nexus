{
  "name": "replace_file_content",
  "args": {
    "AllowMultiple": "false",
    "Description": "\"app.js内の未定義変数（regions）のエラーを修正し、ドラッグによる手動3D回転とスプリング復元ロジックを完全に追加しました。\"",
    "EndLine": "1373",
    "Instruction": "\"Fix undefined regions reference and add mousedown/mousemove/mouseup drag physics.\"",
    "ReplacementContent": "\"  // --- インタラクティブ 3D PARALLAX TILT ＆ DRAG ROTATE ---\\n  const stage = document.querySelector('.map-3d-stage');\\n  const container = document.querySelector('.cyber-map-container');\\n  let isDragging = false;\\n  let startX = 0, startY = 0;\\n  let currentRotateX = 48;\\n  let currentRotateY = 0;\\n\\n  if (stage && container) {\\n      // マウスホバー追従 (微小チルト)\\n      stage.addEventListener('mousemove', (e) => {\\n          if (isDragging) return;\\n          const rect = stage.getBoundingClientRect();\\n          const x = e.clientX - rect.left;\\n          const y = e.clientY - rect.top;\\n          \\n          const pctX = (x / rect.width) - 0.5;\\n          const pctY = (y / rect.height) - 0.5;\\n          \\n          const rotateX = 48 - (pctY * 30);  // Y座標のズレでX軸回転 (上下)\\n          const rotateY = (pctX * 35);       // X座標のズレでY軸回転 (左右)\\n          \\n          container.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg) rotateZ(-12deg)`;\\n      });\\n\\n      stage.addEventListener('mouseleave', () => {\\n          if (isDragging) return;\\n          container.style.transform = `rotateX(48deg) rotateY(0deg) rotateZ(-12deg)`;\\n      });\\n\\n      // マウスドラッグ回転 (物理的なホールド)\\n      stage.addEventListener('mousedown', (e) => {\\n          isDragging = true;\\n          startX = e.clientX;\\n          startY = e.clientY;\\n          stage.style.cursor = 'grabbing';\\n      });\\n\\n      window.addEventListener('mousemove', (e) => {\\n          if (!isDragging) return;\\n          const dx = e.clientX - startX;\\n          const dy = e.clientY - startY;\\n          \\n          currentRotateY += dx * 0.18;\\n          currentRotateX -= dy * 0.18;\\n          \\n          currentRotateX = Math.max(15, Math.min(80, currentRotateX)); // 回転範囲の制限\\n          \\n          container.style.transform = `rotateX(${currentRotateX}deg) rotateY(${currentRotateY}deg) rotateZ(-12deg)`;\\n          \\n          startX = e.client\n<truncated 3641 bytes>",
    "StartLine": "1278",
    "TargetContent": "\"  // --- インタラクティブ 3D PARALLAX TILT ＆ DRAG ROTATE ---\\n  const stage = document.querySelector('.map-3d-stage');\\n  const container = document.querySelector('.cyber-map-container');\\n  let isDragging = false;\\n  let startX = 0, startY = 0;\\n  let currentRotateX = 48;\\n  let currentRotateY = 0;\\n\\n  if (stage && container) {\\n      // マウスホバー追従 (微小チルト)\\n      stage.addEventListener('mousemove', (e) => {\\n          if (isDragging) return;\\n          const rect = stage.getBoundingClientRect();\\n          const x = e.clientX - rect.left;\\n          const y = e.clientY - rect.top;\\n          \\n          const pctX = (x / rect.width) - 0.5;\\n          const pctY = (y / rect.height) - 0.5;\\n          \\n          const rotateX = 48 - (pctY * 30);  // Y座標のズレでX軸回転 (上下)\\n          const rotateY = (pctX * 35);       // X座標 of ズレでY軸回転 (左右)\\n          \\n          container.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg) rotateZ(-12deg)`;\\n      });\\n\\n      stage.addEventListener('mouseleave', () => {\\n          container.style.transform = `rotateX(48deg) rotateY(0deg) rotateZ(-12deg)`;\\n      });\\n  }\\n\\n  // --- イベントバインド ---\\n  regions.forEach(el => {\\n      const regionName = el.getAttribute('data-region');\\n\\n      el.addEventListener('mouseenter', () => {\\n          if (selectedRegion) return;\\n          \\n          // ホバー中の情報を表示\\n          terminalContent.innerHTML = renderScanInfo(regionName);\\n          \\n          // アニメーション発火\\n          const title = document.getElementById('scan-region-title');\\n          if (title) decodeText(title, `■ ${regionName}`);\\n          \\n          const meta = REGION_METADATA[regionName];\\n          if (meta) fillDangerMeter(meta.dangerLevel);\\n      });\\n\\n      el.addEventListener('mouseleave', () => {\\n          if (selectedRegion) return;\\n          \\n          // 初期化\\n          terminalContent.innerHTML = `\\n              \n<truncated 1924 bytes>",
    "TargetFile": "\"/Users/kosako/kaihatsu/dinosaurs website/app.js\"",
    "toolAction": "\"Fixing JS reference and drag logic\"",
    "toolSummary": "\"Fix map logic in app.js\""
  }
}